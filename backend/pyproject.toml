[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "receipt-mng-backend"
version = "0.1.0"
description = "Receipt management backend utilities."
requires-python = ">=3.11"

[tool.setuptools]
packages = ["notion", "app", "app.services"]
